export async function generateQuotationPdf(_quote: any, _items: any[]): Promise<Uint8Array> {
  return new Uint8Array();
}

export async function generateOrderInvoicePdf(_order: any, _items: any[]): Promise<Uint8Array> {
  return new Uint8Array();
}
